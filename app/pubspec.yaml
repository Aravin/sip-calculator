name: sip_calculator
description: All-in-one Financial Calculator with SIP, Lumpsum, SWP, STP, PPF, EMI, FD, RD, Compound Interest, Tax Calculator, Goal Planner, Monte Carlo simulation, AI insights, step-up SIP, LTCG tax, inflation adjustment, NRI currency mode, CSV export, and dark mode.

publish_to: 'none'

version: 5.9.0+39

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^9.0.0
  intl: ^0.19.0
  in_app_review: ^2.0.12
  fl_chart: ^0.69.2
  shared_preferences: ^2.3.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/logo.png"

flutter:
  uses-material-design: true

  fonts:
    - family: IBM Plex Mono
      fonts:
        - asset: assets/fonts/IBM_Plex_Mono/IBMPlexMono-Regular.ttf
        - asset: assets/fonts/IBM_Plex_Mono/IBMPlexMono-Italic.ttf
          style: italic
        - asset: assets/fonts/IBM_Plex_Mono/IBMPlexMono-Bold.ttf
          weight: 700
